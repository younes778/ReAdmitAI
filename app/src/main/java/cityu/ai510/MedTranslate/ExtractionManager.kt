package cityu.ai510.MedTranslate

class ExtractionManager {

    fun extractInfo(id: String): List<String> {
        return listOf(
            "Cardiac size cannot be evaluated",
            "Large left pleural effusion",
            "Small right effusion is new"
        )
    }
}